paper "Monte Carlo Method for Estimating Pi" {

    experiment "Running MC for a fixed set of parameters" 
{

        // Defining parameters
        
        let side = 1.0
        let num_trials = 10000

        // Running steps

        cmd("./example_scripts/my-script.py", side, num_trials)
        
        let other_variable = -12.2

    }

}
